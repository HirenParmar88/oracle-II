DECLARE
	CURSOR MYCUR
	IS
	SELECT EMPNO,ENAME,JOB
	FROM EMP;
	
	VEMPNO NUMBER;
	VENAME VARCHAR2(10);
	VJOB VARCHAR2(10);
BEGIN
	OPEN MYCUR;
	
	LOOP
	FETCH MYCUR INTO VEMPNO,VENAME,VJOB;
	EXIT WHEN MYCUR%NOTFOUND;
	
	DBMS_OUTPUT.LINE( VEMPNO || ' '|| ' '|| VJOB );
	END LOOP;
END;
/